# گزارش جامع و دقیق باگ‌ها، ایرادات و کمبودهای پروژه FlexPro V2

**تاریخ بررسی:** $(date)  
**نسخه پروژه:** 2.0  
**وضعیت:** بررسی کامل کدبیس

---

## 🔴 باگ‌های بحرانی (Critical Bugs)

### 1. مشکل امنیتی XSS در PrintModal
**مکان:** `src/components/PrintModal.tsx:103`  
**مشکل:** استفاده از `dangerouslySetInnerHTML` بدون sanitization کامل  
**خطر:** امکان تزریق کد JavaScript از طریق داده‌های کاربر  
**راه حل:**
```typescript
// استفاده از DOMPurify یا escapeHtml کامل
import DOMPurify from 'dompurify';
dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(data.html) }}
```

### 2. مشکل در مدیریت coach_id برای شاگردان جدید
**مکان:** `src/pages/ClientDashboard.tsx:158`  
**مشکل:** اگر شاگرد جدید باشد و `coach_id` در `profiles` table وجود نداشته باشد، ذخیره با خطا مواجه می‌شود  
**راه حل:** اضافه کردن منطق برای ایجاد خودکار `coach_id` از طریق `user_metadata` یا درخواست از کاربر

### 3. Race Condition در بارگذاری داده‌ها
**مکان:** `src/context/AppContext.tsx:611-662`  
**مشکل:** اگر کاربر قبل از اتمام بارگذاری از Supabase خارج شود، `active` flag ممکن است کار نکند  
**راه حل:** استفاده از `AbortController` برای cancel کردن درخواست‌ها

### 4. مشکل در Drag & Drop - DietPanel
**مکان:** `src/components/DietPanel.tsx:121-123`  
**مشکل:** اگر `active.id` یا `over.id` فرمت صحیح نداشته باشد، `parseInt` ممکن است `NaN` برگرداند  
**وضعیت:** تا حدودی برطرف شده اما نیاز به بررسی بیشتر دارد

### 5. مشکل در Drag & Drop - TrainingPanel
**مکان:** `src/components/TrainingPanel.tsx` (خطوط مشابه)  
**مشکل:** همان مشکل بالا در TrainingPanel  
**وضعیت:** نیاز به بررسی

---

## 🟠 باگ‌های مهم (Important Bugs)

### 6. عدم Validation کامل ورودی‌ها در RegisterPage
**مکان:** `src/pages/RegisterPage.tsx:22`  
**مشکل:**
- نام کاربری ممکن است کاراکترهای خاص داشته باشد
- ایمیل اختیاری است اما Supabase نیاز به ایمیل دارد
- رمز عبور فقط `minLength={6}` دارد که ضعیف است

**راه حل:**
```typescript
// اضافه کردن validation
const usernameRegex = /^[a-zA-Z0-9_]{3,20}$/;
const passwordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/;
```

### 7. مشکل در مدیریت خطا در AuthContext
**مکان:** `src/context/AuthContext.tsx:106-116`  
**مشکل:** خطاها فقط throw می‌شوند و به کاربر نمایش داده نمی‌شوند  
**راه حل:** اضافه کردن toast برای نمایش خطاها

### 8. مشکل در sync داده‌ها بین Supabase و localStorage
**مکان:** `src/context/AppContext.tsx:674-695`  
**مشکل:** اگر Supabase فعال باشد، localStorage استفاده نمی‌شود اما در صورت قطع اتصال، داده‌ها از دست می‌روند  
**راه حل:** استفاده از fallback به localStorage در صورت خطای Supabase

### 9. مشکل در محاسبه لیست خرید هفتگی
**مکان:** `src/components/DietPanel.tsx`  
**مشکل:**
- اگر `activeUser.days` تعریف نشده باشد، محاسبه اشتباه می‌شود
- فقط برای یک هفته محاسبه می‌شود

**راه حل:**
```typescript
const trainingDays = parseInt(String(activeUser.days ?? 3)) || 3;
const restDays = Math.max(0, 7 - trainingDays);
```

### 10. مشکل در Validation ورودی‌های DietPanel
**مکان:** `src/components/DietPanel.tsx:146-180`  
**مشکل:**
- بررسی نمی‌کند که `amount` عدد معتبر باشد
- بررسی نمی‌کند که `info.b` صفر نباشد (تقسیم بر صفر)

**راه حل:**
```typescript
const amountNum = parseFloat(amount);
if (isNaN(amountNum) || amountNum <= 0) {
  toast.error('مقدار باید عدد مثبت باشد');
  return;
}
if (!info || !info.b || info.b === 0) {
  toast.error('اطلاعات غذا نامعتبر است');
  return;
}
```

### 11. مشکل در مدیریت state در ClientDashboard
**مکان:** `src/pages/ClientDashboard.tsx:102-140`  
**مشکل:** `handleProfileChange` و `setProfileDataField` ممکن است state را به درستی به‌روزرسانی نکنند  
**راه حل:** استفاده از functional update برای state

### 12. مشکل در نمایش نقش کاربر در Dashboard
**مکان:** `src/pages/Dashboard.tsx:18-24`  
**مشکل:** اگر `role` و `profile?.role` هر دو null باشند، کاربر نمی‌تواند وارد شود  
**راه حل:** اضافه کردن fallback و loading state بهتر

---

## 🟡 مشکلات منطقی (Logical Issues)

### 13. مشکل در Migration داده‌های قدیمی
**مکان:** `src/context/AppContext.tsx:636-656`  
**مشکل:** اگر کاربر قدیمی `dietRest` نداشته باشد، به آرایه خالی تبدیل می‌شود اما منطق کپی خودکار وجود ندارد  
**راه حل:** اضافه کردن منطق برای کپی خودکار `diet` به `dietRest`

### 14. مشکل در محاسبه ماکروها برای recomp در روز استراحت
**مکان:** `src/components/DietPanel.tsx`  
**مشکل:** برای `recomp` در روز استراحت، ماکروها تنظیم نشده است  
**راه حل:** اضافه کردن منطق برای `recomp` در روز استراحت

### 15. مشکل در نمایش وضعیت مالی
**مکان:** `src/components/UserList.tsx:39-60`  
**مشکل:**
- اگر `startDate` وجود نداشته باشد، همیشه "نامشخص" نشان می‌دهد
- محاسبه `daysLeft` ممکن است برای تاریخ‌های گذشته مشکل داشته باشد

**راه حل:** بهبود منطق محاسبه و نمایش

### 16. مشکل در مدیریت برنامه تمرینی برای warmup/cooldown
**مکان:** `src/components/TrainingPanel.tsx:276-279`  
**مشکل:** `type` برای warmup و cooldown به `undefined` تنظیم می‌شود که ممکن است باعث مشکل در نمایش شود  
**راه حل:** استفاده از type خاص برای warmup/cooldown یا استفاده از `mode` به جای `type`

### 17. مشکل در sync برنامه تمرینی به Supabase
**مکان:** `src/context/AppContext.tsx:718-727`  
**مشکل:** `syncUserToSupabase` فقط در `saveUser` صدا زده می‌شود، اما در `updateActiveUser` صدا زده نمی‌شود  
**راه حل:** اضافه کردن sync در `updateActiveUser`

---

## 🔵 مشکلات UI/UX

### 18. مشکل در Responsive Design
**مکان:** تمام کامپوننت‌ها  
**مشکل:**
- Sidebar در موبایل hidden است اما جایگزین مناسبی وجود ندارد
- برخی فرم‌ها در موبایل به درستی نمایش داده نمی‌شوند
- دکمه‌ها در موبایل ممکن است خیلی کوچک باشند

**راه حل:** بهبود responsive design و اضافه کردن mobile menu

### 19. مشکل در Accessibility
**مکان:** تمام کامپوننت‌ها  
**مشکل:**
- برخی دکمه‌ها `aria-label` ندارند
- برخی input ها label ندارند
- رنگ‌ها ممکن است برای افراد کوررنگ مناسب نباشند
- Navigation با keyboard کامل نیست

**راه حل:**
- اضافه کردن `aria-label` برای تمام دکمه‌ها
- اضافه کردن `label` برای تمام input ها
- بهبود contrast رنگ‌ها
- اضافه کردن keyboard navigation

### 20. مشکل در Loading States
**مکان:** `src/pages/Dashboard.tsx:10-11`  
**مشکل:** Loading state خیلی ساده است و اطلاعات مفیدی به کاربر نمی‌دهد  
**راه حل:** اضافه کردن skeleton loader یا progress indicator

### 21. مشکل در Error Messages
**مکان:** تمام کامپوننت‌ها  
**مشکل:** برخی خطاها فقط در console نمایش داده می‌شوند و به کاربر نشان داده نمی‌شوند  
**راه حل:** استفاده از toast برای نمایش تمام خطاها

### 22. مشکل در Toast Positioning
**مکان:** `src/App.tsx:16`  
**مشکل:** Toast در `bottom-left` است که ممکن است با محتوای صفحه تداخل داشته باشد  
**راه حل:** تغییر به `top-center` یا `top-right`

---

## 🟢 مشکلات عملکردی (Performance Issues)

### 23. مشکل در Re-render غیرضروری
**مکان:** `src/context/AppContext.tsx:675-695`  
**مشکل:** هر تغییر در `users` یا `templates` باعث ذخیره در localStorage می‌شود (حتی با debounce)  
**راه حل:** استفاده از `useMemo` برای بهینه‌سازی

### 24. مشکل در بارگذاری تصاویر تمرینات
**مکان:** `src/data/nasmImages.ts`  
**مشکل:** تمام تصاویر در یک فایل import می‌شوند که باعث افزایش حجم bundle می‌شود  
**راه حل:** استفاده از lazy loading برای تصاویر

### 25. مشکل در Chart.js
**مکان:** `src/components/ProfilePanel.tsx:2-17`  
**مشکل:** Chart.js در هر رندر register می‌شود  
**راه حل:** انتقال register به خارج از کامپوننت یا استفاده از `useMemo`

### 26. مشکل در Drag & Drop Performance
**مکان:** `src/components/DietPanel.tsx`, `src/components/TrainingPanel.tsx`  
**مشکل:** در لیست‌های بزرگ، drag & drop ممکن است کند باشد  
**راه حل:** استفاده از virtualization برای لیست‌های بزرگ

---

## 🔴 مشکلات امنیتی (Security Issues)

### 27. مشکل در XSS (قبلاً ذکر شد)
**مکان:** `src/components/PrintModal.tsx:103`  
**اولویت:** بالا

### 28. مشکل در Validation ورودی‌ها
**مکان:** تمام فرم‌ها  
**مشکل:** برخی ورودی‌ها validate نمی‌شوند و ممکن است داده‌های مخرب وارد شوند  
**راه حل:** اضافه کردن validation برای تمام ورودی‌ها

### 29. مشکل در مدیریت Session
**مکان:** `src/context/AuthContext.tsx`  
**مشکل:** Session timeout مدیریت نمی‌شود  
**راه حل:** اضافه کردن automatic logout پس از timeout

### 30. مشکل در CORS و API Security
**مکان:** `src/lib/supabaseClient.ts`  
**مشکل:** اگر Supabase URL یا key در frontend باشد، ممکن است در معرض خطر باشد  
**راه حل:** استفاده از environment variables و بررسی CORS settings

---

## 🟣 مشکلات علمی (Scientific Issues)

### 31. مشکل در محاسبه BMR
**مکان:** `src/components/UserModal.tsx:83-100`  
**مشکل:**
- اگر `w`, `h`, یا `a` صفر باشد، BMR صفر می‌شود
- میانگین‌گیری ممکن است دقیق نباشد

**راه حل:** بررسی دقیق‌تر ورودی‌ها و استفاده از فرمول‌های دقیق‌تر

### 32. مشکل در محاسبه TDEE
**مکان:** `src/components/UserModal.tsx:100-107`  
**مشکل:**
- `adjustedActivityFactor` ممکن است برای برخی افراد مناسب نباشد
- برای افراد بسیار فعال یا بسیار کم‌تحرک، ممکن است دقیق نباشد

**راه حل:** استفاده از فرمول‌های دقیق‌تر و در نظر گرفتن شرایط خاص

### 33. مشکل در محاسبه ماکروها
**مکان:** `src/components/UserModal.tsx:238-278`  
**مشکل:**
- برای برخی اهداف، نسبت ماکروها ممکن است مناسب نباشد
- برای افراد با شرایط خاص (مثلاً دیابت)، نیاز به تنظیمات خاص است

**راه حل:** اضافه کردن گزینه‌های بیشتر برای تنظیم ماکروها

### 34. مشکل در محاسبه Body Fat با فرمول Navy
**مکان:** `src/components/UserModal.tsx:135-213`  
**مشکل:**
- اگر `waist - neck <= 0` باشد، `Math.log10` خطا می‌دهد
- برای خانم‌ها اگر `hip` وجود نداشته باشد، محاسبه نمی‌شود

**راه حل:** اضافه کردن validation و fallback

---

## 📋 کمبودهای عملکردی (Missing Features)

### 35. عدم وجود Real-time Sync
**مشکل:** تغییرات در یک session به session دیگر sync نمی‌شوند  
**راه حل:** استفاده از Supabase Realtime

### 36. عدم وجود Offline Support
**مشکل:** اگر اتصال اینترنت قطع شود، کاربر نمی‌تواند کار کند  
**راه حل:** استفاده از Service Worker و Cache API

### 37. عدم وجود Export/Import برای تک کاربر
**مشکل:** کاربر نمی‌تواند فقط اطلاعات خودش را export/import کند  
**راه حل:** اضافه کردن قابلیت export/import برای تک کاربر

### 38. عدم وجود Search در لیست تمرینات
**مشکل:** در لیست تمرینات، جستجو وجود ندارد  
**راه حل:** اضافه کردن search functionality

### 39. عدم وجود Filter در لیست شاگردان
**مشکل:** در لیست شاگردان، فقط search وجود دارد و filter وجود ندارد  
**راه حل:** اضافه کردن filter بر اساس وضعیت مالی، سن، و غیره

### 40. عدم وجود Notification System
**مشکل:** کاربر از تغییرات یا رویدادهای مهم مطلع نمی‌شود  
**راه حل:** اضافه کردن notification system

### 41. عدم وجود Analytics/Dashboard
**مشکل:** مربی نمی‌تواند آمار و تحلیل از شاگردانش ببیند  
**راه حل:** اضافه کردن analytics dashboard

### 42. عدم وجود Multi-language Support
**مشکل:** فقط فارسی پشتیبانی می‌شود  
**راه حل:** اضافه کردن i18n support

---

## 🐛 مشکلات TypeScript

### 43. استفاده از `any` در چندین جا
**مکان:** `src/pages/ClientDashboard.tsx`, `src/components/DietPanel.tsx`  
**مشکل:** استفاده از `any` باعث از دست رفتن type safety می‌شود  
**راه حل:** تعریف type های دقیق‌تر

### 44. عدم وجود Type Guards
**مکان:** تمام کامپوننت‌ها  
**مشکل:** Type guards برای بررسی null/undefined وجود ندارد  
**راه حل:** اضافه کردن type guards

---

## 📝 مشکلات Documentation

### 45. عدم وجود JSDoc Comments
**مشکل:** توابع و کامپوننت‌ها documentation ندارند  
**راه حل:** اضافه کردن JSDoc comments

### 46. عدم وجود README کامل
**مشکل:** README موجود اطلاعات کافی ندارد  
**راه حل:** اضافه کردن documentation کامل

---

## 🔧 مشکلات Configuration

### 47. مشکل در Environment Variables
**مکان:** `src/lib/supabaseClient.ts`  
**مشکل:** اگر environment variables وجود نداشته باشند، خطای واضحی نمایش داده نمی‌شود  
**راه حل:** اضافه کردن validation و error message واضح

### 48. مشکل در Build Configuration
**مکان:** `vite.config.ts` (اگر وجود دارد)  
**مشکل:** نیاز به بررسی optimization settings  
**راه حل:** بهینه‌سازی build configuration

---

## 📊 خلاصه آمار

### باگ‌های بحرانی: 5 مورد
### باگ‌های مهم: 7 مورد
### مشکلات منطقی: 5 مورد
### مشکلات UI/UX: 5 مورد
### مشکلات عملکردی: 4 مورد
### مشکلات امنیتی: 4 مورد
### مشکلات علمی: 4 مورد
### کمبودهای عملکردی: 8 مورد
### مشکلات TypeScript: 2 مورد
### مشکلات Documentation: 2 مورد
### مشکلات Configuration: 2 مورد

**جمع کل: 48 مورد**

---

## 🎯 اولویت‌بندی

### اولویت 1 (فوری):
1. مشکل امنیتی XSS در PrintModal
2. مشکل در مدیریت coach_id
3. مشکل در Validation ورودی‌ها
4. مشکل در مدیریت خطا

### اولویت 2 (مهم):
5. مشکل در sync داده‌ها
6. مشکل در Responsive Design
7. مشکل در Accessibility
8. مشکل در Performance

### اولویت 3 (بهبود):
9. کمبودهای عملکردی
10. مشکلات علمی
11. مشکلات Documentation

---

**نکته:** این گزارش بر اساس بررسی دقیق کدبیس تهیه شده است. توصیه می‌شود که مشکلات با اولویت 1 و 2 در اسرع وقت برطرف شوند.

