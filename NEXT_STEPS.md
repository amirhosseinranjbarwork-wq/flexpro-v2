# مراحل بعدی و چیزهایی که نیاز است

## ✅ کارهای انجام شده

1. ✅ بازنویسی کامل بانک داده‌های تمرینی
2. ✅ بازنویسی کامل بانک داده‌های غذایی
3. ✅ بازنویسی کامل بانک داده‌های مکمل
4. ✅ طراحی پارامترهای حرفه‌ای برای تمرینات
5. ✅ طراحی پارامترهای حرفه‌ای برای برنامه غذایی
6. ✅ طراحی پارامترهای حرفه‌ای برای مکمل‌ها
7. ✅ به‌روزرسانی DietPanel
8. ✅ به‌روزرسانی SupplementsPanel

## 🔄 کارهای در حال انجام

### 1. به‌روزرسانی useExercises Hook
**فایل:** `src/hooks/useExercises.ts`

**مشکل:** 
- Fallback exercises فقط 20 تمرین اول را استفاده می‌کند
- باید از تمام تمرینات جدید استفاده کند

**راه‌حل:**
```typescript
// تبدیل تمام exercises به فرمت مورد نیاز
const fallbackExercises = exercises.map(exercise => ({
  id: exercise.id,
  name: exercise.name,
  muscle_group: exercise.muscleGroup,
  sub_muscle_group: exercise.subMuscleGroup || null,
  equipment: exercise.equipment,
  type: exercise.type,
  mechanics: exercise.mechanics,
  description: exercise.description,
  category: exercise.type === 'cardio' ? 'cardio' : 
             exercise.type === 'warmup' ? 'warmup' :
             exercise.type === 'cooldown' ? 'cooldown' :
             exercise.type === 'corrective' ? 'corrective' : 'bodybuilding',
  difficulty: exercise.difficulty
}));
```

### 2. بهبود فیلتر کردن تمرینات در WorkoutBuilder
**فایل:** `src/components/TrainingPanel/WorkoutBuilder.tsx`

**مشکل:**
- فیلترها بر اساس `category` و `type` هستند
- باید از `muscleGroup` و `subMuscleGroup` هم استفاده کند

**راه‌حل:**
```typescript
// بهبود فیلتر مقاومتی
const resistanceExercises = useMemo(() => 
  exercises.filter(ex => 
    ex.type === 'resistance' ||
    (ex.muscleGroup && ex.muscleGroup !== 'قلبی') ||
    (!ex.type && !ex.muscleGroup)
  ), [exercises]);

// بهبود فیلتر کاردیو
const cardioExercises = useMemo(() => 
  exercises.filter(ex => 
    ex.type === 'cardio' ||
    ex.muscleGroup === 'قلبی' ||
    ex.category === 'cardio'
  ), [exercises]);
```

### 3. به‌روزرسانی database.ts
**فایل:** `src/lib/database.ts`

**وضعیت:** ✅ قبلاً به‌روزرسانی شده
- از `localExercises` و `localFoods` استفاده می‌کند
- تابع `convertLocalExerciseToDB` وجود دارد

**نیاز به بررسی:**
- آیا تمام فیلدهای جدید را پوشش می‌دهد؟
- آیا `muscleGroup` و `subMuscleGroup` به درستی تبدیل می‌شوند؟

## 📋 کارهای باقی‌مانده

### 1. تست کامل سیستم
- [ ] تست جستجوی تمرینات
- [ ] تست فیلتر کردن تمرینات
- [ ] تست اضافه کردن تمرین به برنامه
- [ ] تست جستجوی غذاها
- [ ] تست اضافه کردن غذا به برنامه غذایی
- [ ] تست جستجوی مکمل‌ها
- [ ] تست اضافه کردن مکمل به برنامه

### 2. به‌روزرسانی کامپوننت‌های تمرینی
- [ ] بررسی `ResistanceForm` - آیا از پارامترهای جدید استفاده می‌کند؟
- [ ] بررسی `CardioForm` - آیا از `CardioParameters` استفاده می‌کند؟
- [ ] بررسی `CorrectiveForm` - آیا از `CorrectiveParameters` استفاده می‌کند؟
- [ ] بررسی `PlyometricForm` - آیا از `PlyometricParameters` استفاده می‌کند؟

### 3. به‌روزرسانی Store ها
- [ ] بررسی `useWorkoutStore` - آیا از داده‌های جدید استفاده می‌کند؟
- [ ] بررسی `useDietStore` - آیا از داده‌های جدید استفاده می‌کند؟
- [ ] بررسی `useSupplementStore` - آیا از داده‌های جدید استفاده می‌کند؟

### 4. بهبود UI/UX
- [ ] نمایش پارامترهای حرفه‌ای در فرم‌ها
- [ ] اضافه کردن tooltip برای پارامترها
- [ ] بهبود جستجو با فیلترهای پیشرفته
- [ ] نمایش meal timing برای غذاها
- [ ] نمایش optimal timing برای مکمل‌ها

### 5. مستندسازی
- [ ] به‌روزرسانی README اصلی
- [ ] ایجاد مثال‌های استفاده از پارامترها
- [ ] مستندسازی API های جدید

## 🎯 اولویت‌ها

### اولویت بالا (فوری)
1. ✅ به‌روزرسانی `useExercises` hook
2. ✅ بهبود فیلترها در `WorkoutBuilder`
3. ✅ تست اولیه سیستم

### اولویت متوسط
1. به‌روزرسانی فرم‌های تمرینی
2. به‌روزرسانی Store ها
3. بهبود UI/UX

### اولویت پایین
1. مستندسازی کامل
2. مثال‌های استفاده
3. بهینه‌سازی عملکرد

## 🔍 بررسی‌های لازم

### بررسی سازگاری
- [ ] آیا تمام کامپوننت‌ها با ساختار جدید سازگارند؟
- [ ] آیا TypeScript errors وجود دارد؟
- [ ] آیا runtime errors وجود دارد؟

### بررسی عملکرد
- [ ] آیا بارگذاری داده‌ها سریع است؟
- [ ] آیا جستجو سریع است؟
- [ ] آیا فیلترها بهینه هستند؟

### بررسی کامل بودن داده‌ها
- [ ] آیا تمام تمرینات نمایش داده می‌شوند؟
- [ ] آیا تمام غذاها نمایش داده می‌شوند؟
- [ ] آیا تمام مکمل‌ها نمایش داده می‌شوند؟

## 📝 نکات مهم

1. **سازگاری با کد موجود:** 
   - Helper functions برای سازگاری ایجاد شده‌اند
   - فایل‌های قدیمی هنوز وجود دارند (برای fallback)

2. **Migration Strategy:**
   - داده‌های جدید به صورت تدریجی استفاده می‌شوند
   - اگر API در دسترس نباشد، از داده‌های جدید استفاده می‌شود
   - اگر داده‌های جدید در دسترس نباشد، از داده‌های قدیمی استفاده می‌شود

3. **Testing:**
   - ابتدا تست‌های اولیه
   - سپس تست‌های کامل
   - در نهایت تست‌های integration

## 🚀 شروع کار

برای شروع، این مراحل را دنبال کنید:

1. **به‌روزرسانی useExercises:**
   ```bash
   # فایل را باز کنید
   src/hooks/useExercises.ts
   ```

2. **بهبود WorkoutBuilder:**
   ```bash
   # فایل را باز کنید
   src/components/TrainingPanel/WorkoutBuilder.tsx
   ```

3. **تست سیستم:**
   ```bash
   npm run dev
   # بررسی کنید که همه چیز کار می‌کند
   ```

4. **بررسی خطاها:**
   ```bash
   npm run lint
   npm run type-check
   ```

## 📞 در صورت نیاز به کمک

اگر مشکلی پیش آمد:
1. خطاها را بررسی کنید
2. Console را چک کنید
3. TypeScript errors را بررسی کنید
4. از helper functions استفاده کنید

