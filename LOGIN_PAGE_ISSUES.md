# مشکلات صفحه لاگین - تحلیل کامل

## 🔴 مشکلات ظاهری (UI/UX)

1. **دکمه خروج از حساب در پایین صفحه**
   - مشکل: دکمه "خروج از حساب" حتی وقتی کاربر لاگین نکرده نمایش داده می‌شود
   - خط: 135-145
   - راه حل: باید فقط وقتی `user` وجود دارد نمایش داده شود

2. **عدم نمایش خطاهای واضح**
   - مشکل: خطاها فقط در toast نمایش داده می‌شوند، نه در خود فرم
   - راه حل: اضافه کردن نمایش خطا زیر هر فیلد

3. **عدم وجود loading state مناسب**
   - مشکل: فقط دکمه loading می‌شود، اما فیلدها disable نمی‌شوند
   - راه حل: disable کردن فیلدها هنگام loading

4. **عدم وجود نمایش موفقیت واضح‌تر**
   - مشکل: فقط toast نمایش داده می‌شود
   - راه حل: می‌توان یک animation موفقیت اضافه کرد

## ⚠️ مشکلات عملکردی

1. **استفاده از `useApp`**
   - مشکل: در refactoring جدید باید از `useUI` استفاده شود
   - خط: 3, 10
   - راه حل: تغییر به `useUI`

2. **تب‌های مربی/شاگرد بی‌معنی**
   - مشکل: تب‌های "مربی" و "شاگرد" هیچ تفاوتی در لاگین ایجاد نمی‌کنند
   - راه حل: باید role را بررسی کند و فقط کاربران با role مناسب را اجازه ورود دهد

3. **عدم بررسی role بعد از لاگین**
   - مشکل: بعد از لاگین موفق، role کاربر بررسی نمی‌شود
   - راه حل: استفاده از `useRole` hook و بررسی role

4. **عدم redirect بر اساس role**
   - مشکل: همه کاربران به `/dashboard` redirect می‌شوند
   - راه حل: مربی‌ها به `/dashboard?role=coach` و شاگردها به `/dashboard?role=client`

5. **عدم مدیریت خطاهای خاص**
   - مشکل: خطاهای خاص مثل "ایمیل تایید نشده" مدیریت نمی‌شوند
   - راه حل: اضافه کردن handling برای خطاهای خاص

6. **عدم وجود "فراموشی رمز عبور"**
   - مشکل: کاربر نمی‌تواند رمز عبور خود را بازیابی کند
   - راه حل: اضافه کردن لینک "فراموشی رمز عبور"

7. **عدم وجود "Remember Me"**
   - مشکل: کاربر باید هر بار لاگین کند
   - راه حل: اضافه کردن checkbox "Remember Me"

8. **عدم پاک‌سازی فیلدها بعد از خطا**
   - مشکل: بعد از خطا، فیلدها پاک نمی‌شوند
   - راه حل: پاک کردن فیلد password بعد از خطا

## 🚨 مشکلات منطقی و امنیتی

1. **عدم استفاده از `useRole` hook**
   - مشکل: از hook جدید `useRole` استفاده نمی‌شود
   - راه حل: استفاده از `useRole` برای بررسی role

2. **عدم اعتبارسنجی role قبل از redirect**
   - مشکل: role کاربر قبل از redirect بررسی نمی‌شود
   - راه حل: بررسی role و redirect مناسب

3. **عدم مدیریت session timeout**
   - مشکل: session timeout مدیریت نمی‌شود
   - راه حل: اضافه کردن handling برای session timeout

4. **عدم وجود rate limiting**
   - مشکل: امکان brute force attack وجود دارد
   - راه حل: اضافه کردن rate limiting (در backend)

5. **عدم وجود captcha**
   - مشکل: امکان bot attack وجود دارد
   - راه حل: اضافه کردن captcha (اختیاری)

6. **عدم بررسی role واقعی کاربر**
   - مشکل: تب "مربی" یا "شاگرد" فقط UI است و role واقعی را بررسی نمی‌کند
   - راه حل: بررسی role از profile یا metadata

## 📋 لیست اصلاحات پیشنهادی

### اولویت بالا (Critical)
- [ ] تغییر `useApp` به `useUI`
- [ ] بررسی role بعد از لاگین موفق
- [ ] Redirect بر اساس role
- [ ] پاک کردن دکمه خروج وقتی کاربر لاگین نکرده

### اولویت متوسط (Important)
- [ ] اضافه کردن "فراموشی رمز عبور"
- [ ] اضافه کردن "Remember Me"
- [ ] بهبود نمایش خطاها در فرم
- [ ] Disable کردن فیلدها هنگام loading

### اولویت پایین (Nice to have)
- [ ] اضافه کردن captcha
- [ ] بهبود animation موفقیت
- [ ] اضافه کردن rate limiting (backend)

