# FlexPro Local Backend Configuration
# This is a local-only setup using SQLite database

# Environment
ENVIRONMENT=development

# Server
HOST=0.0.0.0
PORT=8000

# Security (change these in production)
SECRET_KEY=flexpro-local-secret-key-change-this-in-production

# Logging
LOG_LEVEL=INFO

# Optional: AI Features (leave empty if not using)
# OPENAI_API_KEY=sk-...

# Note: Database is automatically configured to use SQLite
# No external database configuration needed!
