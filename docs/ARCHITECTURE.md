# 🏗️ FlexPro v2 - Architecture Diagrams

## System Architecture After Refactoring

```
┌─────────────────────────────────────────────────────────────────────┐
│                         React Application                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              ReactQueryProvider (main.tsx)                  │  │
│  │         Wraps entire app with React Query support            │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                           │                                         │
│                           ▼                                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    App Component                             │  │
│  │  ├─ AuthProvider (Authentication)                           │  │
│  │  ├─ AppProvider (Global State)                              │  │
│  │  └─ Router (Page Navigation)                                │  │
│  │     ├─ LoginPage                                            │  │
│  │     ├─ Dashboard                                            │  │
│  │     └─ TrainingPanel ◄───────┐                             │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## TrainingPanel Component Architecture

### Before Refactoring (Monolithic)
```
┌──────────────────────────────────────┐
│     TrainingPanel.tsx (852 lines)    │
├──────────────────────────────────────┤
│ - Day selection logic                │
│ - Exercise form logic                │
│ - Table rendering logic              │
│ - Drag-and-drop logic                │
│ - All state management               │
│ - All business logic                 │
│ - Error handling                     │
│ - Loading states                     │
│ - Everything mixed together ❌       │
└──────────────────────────────────────┘
```

### After Refactoring (Modular)
```
┌─────────────────────────────────────────────────────────────────┐
│                    TrainingPanel.tsx (250 lines)               │
│              (Orchestrator/Container Component)                │
├─────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              WorkoutDayTabs.tsx (59 lines)            │  │
│  │  ┌──────────────────────────────────────────────────┐ │  │
│  │  │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │ 7 (Day selector tabs)  │ │  │
│  │  │(5)│(3)│(2)│(4)│(1)│(0)│(6) (Exercise count)   │ │  │
│  │  └──────────────────────────────────────────────────┘ │  │
│  │  ✨ Memoized for performance                         │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │            AddExerciseForm.tsx (456 lines)            │  │
│  │  ┌──────────────────────────────────────────────────┐ │  │
│  │  │ Mode selector [Resist] [Cardio] [Corrective]    │ │  │
│  │  │ Search field [_________________]                │ │  │
│  │  │ Exercise type selector                           │ │  │
│  │  │ Sets/Reps/Rest inputs                           │ │  │
│  │  │ System type selector (conditional)               │ │  │
│  │  │ [ADD EXERCISE] button                           │ │  │
│  │  └──────────────────────────────────────────────────┘ │  │
│  │  ✨ Complex form logic encapsulated                │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │         Exercise Table (via ExerciseRow)             │  │
│  │  ┌──────────────────────────────────────────────────┐ │  │
│  │  │ 🟰 │ Bench Press │ 4 │ 8-10 │ 120s │ Strength │ │  │
│  │  │ 🟰 │ Dumbell Row │ 4 │ 8-10 │ 120s │ Strength │ │  │
│  │  │ 🟰 │ Pull-ups    │ 3 │ 6-8  │ 90s  │ Strength │ │  │
│  │  └──────────────────────────────────────────────────┘ │  │
│  │  ✨ Sortable rows with @dnd-kit                     │  │
│  │  ✨ Each row memoized                               │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: User Action to Database

### Scenario: User adds a new exercise

```
┌─ USER CLICKS [ADD EXERCISE] ─┐
│                              │
▼                              │
┌──────────────────────────────────────────┐
│    AddExerciseForm Component             │
│  - Validates form input                  │
│  - Creates WorkoutItem object            │
└──────────────────────────────────────────┘
│
▼
┌──────────────────────────────────────────┐
│    onAddExercise() callback              │
│  - Passed from TrainingPanel             │
│  - Updates activeUser state              │
│  - Calls onUpdateUser()                  │
└──────────────────────────────────────────┘
│
▼
┌──────────────────────────────────────────┐
│    onUpdateUser() (from props)           │
│  - Calls Supabase API                    │
│  - Saves to database                     │
└──────────────────────────────────────────┘
│
▼
┌──────────────────────────────────────────┐
│    queryClient.invalidateQueries()       │
│  - Invalidates 'exercises' cache         │
│  - Triggers refetch                      │
└──────────────────────────────────────────┘
│
▼
┌──────────────────────────────────────────┐
│    useExercisesByMuscleGroup() hook      │
│  - React Query fetches fresh data        │
│  - Returns updated list                  │
└──────────────────────────────────────────┘
│
▼
┌──────────────────────────────────────────┐
│    Component Re-renders                  │
│  - Table shows new exercise              │
│  - User sees confirmation                │
└──────────────────────────────────────────┘
```

---

## React Query Data Fetching Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Component                                    │
│  const { data, isLoading, error } =                            │
│    useExercisesByMuscleGroup('chest');                         │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                  useExercisesByMuscleGroup Hook                │
│  • Manages query state                                         │
│  • Handles caching (10 min stale time)                        │
│  • Automatic retry on failure                                 │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
                 ┌─────────┴─────────┐
                 │                   │
        ┌────────▼────────┐  ┌───────▼───────┐
        │ Cache Valid?    │  │ Still Cached? │
        │ (< 10 min)      │  │ (< 30 min)    │
        └────────┬────────┘  └───────┬───────┘
                 │ YES               │ YES
                 ▼                   ▼
        ┌────────────────┐  ┌─────────────────┐
        │ Return cached  │  │ Return stale    │
        │ data (instant) │  │ data (instant)  │
        │                │  │ + Refetch       │
        └────────────────┘  │ in background   │
                            └─────────────────┘
                 │ NO              │ NO
                 └────────┬────────┘
                          ▼
        ┌───────────────────────────────────────┐
        │    Fetch from Supabase                │
        │  • Search exercises                   │
        │  • Apply filters (muscle_group)       │
        │  • Return paginated results           │
        └───────────────────┬───────────────────┘
                            ▼
        ┌───────────────────────────────────────┐
        │    Cache Response                     │
        │  • Store in queryClient               │
        │  • Set staleTime: 10 min              │
        │  • Set gcTime: 30 min                │
        └───────────────────┬───────────────────┘
                            ▼
        ┌───────────────────────────────────────┐
        │    Return Data to Component           │
        │  • Component re-renders               │
        │  • UI updates with new exercises      │
        └───────────────────────────────────────┘
```

---

## Database Schema Architecture

```
┌─────────────────────────────────────────────────────┐
│            SUPABASE POSTGRESQL DATABASE             │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │         exercises TABLE                    │   │
│  ├─────────────────────────────────────────────┤   │
│  │ id                   UUID (Primary Key)     │   │
│  │ name                 VARCHAR                │   │
│  │ muscle_group         VARCHAR (Indexed)      │   │
│  │ sub_muscle_group     VARCHAR                │   │
│  │ equipment            VARCHAR                │   │
│  │ type                 VARCHAR                │   │
│  │ mechanics            VARCHAR                │   │
│  │ description          TEXT                   │   │
│  │ created_at           TIMESTAMP              │   │
│  │ updated_at           TIMESTAMP              │   │
│  │                                             │   │
│  │ Indexes:                                    │   │
│  │ • GIN index on name (full-text search)      │   │
│  │ • Index on muscle_group (filtering)         │   │
│  │                                             │   │
│  │ RLS Policies:                               │   │
│  │ • SELECT: Allow all authenticated users     │   │
│  │ • INSERT: Admin only                        │   │
│  │ • UPDATE: Admin only                        │   │
│  │ • DELETE: Admin only                        │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │         foods TABLE                        │   │
│  ├─────────────────────────────────────────────┤   │
│  │ id                   UUID (Primary Key)     │   │
│  │ name                 VARCHAR                │   │
│  │ category             VARCHAR (Indexed)      │   │
│  │ unit                 VARCHAR                │   │
│  │ calories             DECIMAL(8,2)           │   │
│  │ protein              DECIMAL(8,2)           │   │
│  │ carbs                DECIMAL(8,2)           │   │
│  │ fat                  DECIMAL(8,2)           │   │
│  │ base_amount          DECIMAL(8,2)           │   │
│  │ created_at           TIMESTAMP              │   │
│  │                                             │   │
│  │ Indexes:                                    │   │
│  │ • GIN index on name (full-text search)      │   │
│  │ • Index on category (filtering)             │   │
│  │                                             │   │
│  │ RLS Policies:                               │   │
│  │ • SELECT: Allow all users                   │   │
│  │ • INSERT: Admin only                        │   │
│  │ • UPDATE: Admin only                        │   │
│  │ • DELETE: Admin only                        │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │  RPC FUNCTIONS (For Efficient Querying)    │   │
│  ├─────────────────────────────────────────────┤   │
│  │                                             │   │
│  │ search_exercises(                           │   │
│  │   p_query TEXT DEFAULT '',                 │   │
│  │   p_muscle_group VARCHAR DEFAULT '',       │   │
│  │   p_limit INT DEFAULT 20                   │   │
│  │ ) RETURNS TABLE(...)                       │   │
│  │                                             │   │
│  │ search_foods(                               │   │
│  │   p_query TEXT DEFAULT '',                 │   │
│  │   p_category VARCHAR DEFAULT '',           │   │
│  │   p_limit INT DEFAULT 20                   │   │
│  │ ) RETURNS TABLE(...)                       │   │
│  │                                             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## State Management Flow

### Before: Manual State Management ❌
```
Component State
    ↓
useState(exercises) ──────────┐
useEffect(() => {             ├─► Manual sync issues
  fetch() → setExercises()    │   - Race conditions
}) ────────────────────────────┘   - Stale data
                                    - Duplicate requests
                                    - Memory leaks
```

### After: React Query ✅
```
Component
    ↓
useExercisesByMuscleGroup()
    ↓
React Query Cache
    ├─ Auto-manages stale state
    ├─ Auto-retry on failure
    ├─ Auto-garbage collection
    ├─ Prevents duplicates
    └─ Handles race conditions
    ↓
Supabase Database
```

---

## Component Hierarchy

```
App
├── AuthProvider
│   └── Router
│       ├── PublicRoutes
│       │   ├── LoginPage
│       │   └── RegisterPage
│       │
│       └── PrivateRoutes
│           ├── Dashboard
│           │   ├── ClientDashboard
│           │   └── CoachDashboard
│           │
│           └── TrainingPanel ◄────────┐ MAIN REFACTORING
│               ├── WorkoutDayTabs     ├─────────────────
│               ├── AddExerciseForm    │ (4 new components)
│               │   └── ExerciseSearchBar (integrated)
│               │
│               └── Exercise Table
│                   ├── ExerciseRow
│                   │   ├── DragHandle (@dnd-kit)
│                   │   ├── ExerciseDetails
│                   │   └── DeleteButton
│                   ├── ExerciseRow
│                   └── ExerciseRow
```

---

## Data Migration Process

```
Stage 1: Preparation
  ├─ Create Supabase tables (SQL migration)
  └─ Create batch migration script

Stage 2: Migration
  TypeScript Files (Local)
      ↓
  ┌─────────────────────┐
  │ resistanceExercises │ ──┐
  │ (nested objects)    │  │
  └─────────────────────┘  │
                           │
  ┌─────────────────────┐  │
  │ correctiveExercises │──┼──► Migration Script
  │ (arrays)            │  │     (Node.js)
  └─────────────────────┘  │
                           │
  ┌─────────────────────┐  │
  │ foodData            │──┘
  │ (complex objects)   │
  └─────────────────────┘

Stage 3: Processing
  Migration Script
      ├─ Parse TypeScript exports
      ├─ Normalize data format
      ├─ Batch 100 records per request
      ├─ Error handling & recovery
      └─ Progress reporting

Stage 4: Storage
  Batch Insert
      ├─ exercises table (500+ rows)
      ├─ foods table (1000+ rows)
      └─ Create indexes

Stage 5: Verification
  ┌────────────────┐
  │ SELECT COUNT(*)│ ✓ exercises: 550
  │ FROM exercises │ ✓ foods: 1200
  └────────────────┘

Stage 6: Application
  React Query Hooks
      ├─ useExercisesByType()
      ├─ useExercisesByMuscleGroup()
      ├─ useFoodsQuery()
      └─ Fetch from Supabase ✓
```

---

## Performance Optimization Pipeline

```
┌───────────────────────────────────────────────────┐
│ User Interaction                                  │
│ (e.g., Selecting Muscle Group)                   │
└───────────────┬─────────────────────────────────┘
                │
                ▼
        ┌───────────────┐
        │ React.memo    │ ◄─── Prevent re-renders
        │ optimization  │      of unchanged
        └───────────────┘      components
                │
                ▼
        ┌───────────────┐
        │ useCallback   │ ◄─── Memoize callbacks
        │ memoization   │
        └───────────────┘
                │
                ▼
        ┌───────────────┐
        │ React Query   │ ◄─── Check cache
        │ Cache Check   │      (10 min stale)
        └───────┬───────┘
                │
        ┌───────┴───────┐
        │               │
      YES              NO
        │               │
        ▼               ▼
    Return Stale   Fetch New Data
    Data (instant) (background)
        │               │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │ Update UI     │ ◄─── Component renders
        │ (re-render)   │      with latest data
        └───────────────┘
```

---

## Error Handling Architecture

```
                 ┌─────────────────┐
                 │  User Action    │
                 └────────┬────────┘
                          │
                          ▼
            ┌─────────────────────────────┐
            │ Component Tries Operation   │
            └────────┬────────────────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │ Does it work?              │
        └────┬───────────────────┬───┘
             │                   │
           YES                   NO
             │                   │
             ▼                   ▼
        ┌─────────┐     ┌──────────────────┐
        │ Success │     │ React Query      │
        │ Message │     │ Retry Logic      │
        └─────────┘     │ (attempt 1x)     │
                        └────┬──────────┬──┘
                             │          │
                          Success    Failure
                             │          │
                             ▼          ▼
                        ┌─────────┐ ┌──────────┐
                        │ Success │ │ Error    │
                        │ Message │ │ Message  │
                        └─────────┘ │ to User  │
                                    │ + Retry  │
                                    │ Button   │
                                    └──────────┘
```

---

## File Dependencies Graph

```
main.tsx
├── queryClient.ts ◄────────────────────┐
│   └── @tanstack/react-query           │
│                                       │
├── App.tsx                             │
│   ├── TrainingPanel.tsx               │
│   │   ├── WorkoutDayTabs.tsx ◄────────┤─────────────┐
│   │   ├── ExerciseRow.tsx             │             │
│   │   │   └── @dnd-kit/sortable       │             │
│   │   │                               │             │
│   │   ├── AddExerciseForm.tsx         │             │
│   │   │   └── useDatabaseQueries.ts ◄─┤─────────────┤────┐
│   │   │       └── useDebounce.ts      │             │    │
│   │   │                               │             │    │
│   │   ├── useExerciseSearch.ts        │             │    │
│   │   │   └── useDebounce.ts          │             │    │
│   │   │                               │             │    │
│   │   └── ClientDashboard.tsx ◄───────┤─────────────┤────┤
│   │       └── useWorkoutLog.ts        │             │    │
│   │                                   │             │    │
│   └── Supabase Database ◄─────────────┘             │    │
│                                                     │    │
├── supabaseClient.ts ◄──────────────────────────────┘    │
│   └── Supabase API                                      │
│                                                         │
└── Database Tables ◄────────────────────────────────────┘
    ├── exercises table
    └── foods table
```

---

## Deployment Flow

```
Development
    ↓
┌─────────────────────────────┐
│ Run locally                 │
│ npm run dev                 │
└─────────────────────────────┘
    ↓
┌─────────────────────────────┐
│ Test all components         │
│ Verify caching works        │
│ Check database queries      │
└─────────────────────────────┘
    ↓
┌─────────────────────────────┐
│ Build for production        │
│ npm run build               │
└─────────────────────────────┘
    ↓
┌─────────────────────────────┐
│ Check bundle size           │
│ Verify types compile        │
└─────────────────────────────┘
    ↓
┌─────────────────────────────┐
│ Git commit                  │
│ Push to main branch         │
└─────────────────────────────┘
    ↓
Production
```

---

**Generated**: January 16, 2025
**Project**: FlexPro v2 Architecture Documentation
**Status**: Complete ✅
