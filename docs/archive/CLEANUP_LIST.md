# 🧹 لیست فایل‌های اضافی برای حذف

**تاریخ:** $(date)  
**وضعیت:** آماده برای تأیید

---

## ✅ فایل‌های مطمئن برای حذف (100% استفاده نشده)

### 1. پوشه `src/data/legacy/` (6 فایل)
**مسیر:** `src/data/legacy/`

**فایل‌ها:**
- `cardioExercises.ts`
- `correctiveExercises.ts`
- `correctiveExercisesComplete.ts`
- `nasmImages.ts`
- `nasmPrograms.ts`
- `warmupCooldown.ts`

**دلیل:** هیچ استفاده‌ای از این فایل‌ها در کل پروژه پیدا نشد. داده‌های تمرین در `src/data/exercises.ts` موجود است.

**تأیید شده:** ✅

---

### 2. فایل `landingpage.txt`
**مسیر:** `landingpage.txt` (ریشه پروژه)

**دلیل:** این فایل فقط یک فایل متنی است که کد Landing Page را دارد. Landing Page واقعی در `src/pages/LandingPage.tsx` پیاده‌سازی شده و استفاده می‌شود.

**تأیید شده:** ✅

---

### 3. فایل `FLEX APP - Shortcut.lnk`
**مسیر:** `FLEX APP - Shortcut.lnk` (ریشه پروژه)

**دلیل:** این یک فایل shortcut ویندوز است که نباید در repository باشد. باید در `.gitignore` اضافه شود.

**تأیید شده:** ✅

---

## ⚠️ فایل‌های نیاز به تأیید

### 4. پوشه `test-app/` (کل پوشه)
**مسیر:** `test-app/`

**محتوای پوشه:**
- یک پروژه Vite جداگانه
- `package.json`, `tsconfig.json`, `vite.config.ts`
- `src/` با فایل‌های نمونه
- `node_modules/`

**وضعیت:** هیچ ارجاعی به این پوشه در کد اصلی پیدا نشد. به نظر می‌رسد یک پروژه تست جداگانه است.

**سوال:** آیا این پوشه هنوز نیاز است یا می‌تواند حذف شود؟

**پیشنهاد:** اگر برای تست استفاده نمی‌شود، حذف شود.

---

## 📋 خلاصه

| # | فایل/پوشه | نوع | وضعیت | اندازه تقریبی |
|---|-----------|-----|--------|----------------|
| 1 | `src/data/legacy/` | پوشه (6 فایل) | ✅ مطمئن | ~50 KB |
| 2 | `landingpage.txt` | فایل متنی | ✅ مطمئن | ~20 KB |
| 3 | `FLEX APP - Shortcut.lnk` | فایل shortcut | ✅ مطمئن | ~1 KB |
| 4 | `test-app/` | پوشه کامل | ⚠️ نیاز به تأیید | ~10 MB (با node_modules) |

**جمع کل:** ~10 MB (بیشتر از `test-app/node_modules`)

---

## 🎯 اقدامات پیشنهادی

### فاز 1: حذف فایل‌های مطمئن
1. ✅ حذف `src/data/legacy/` (کل پوشه)
2. ✅ حذف `landingpage.txt`
3. ✅ حذف `FLEX APP - Shortcut.lnk`
4. ✅ اضافه کردن `*.lnk` به `.gitignore`

### فاز 2: حذف test-app (بعد از تأیید)
5. ⚠️ حذف `test-app/` (اگر تأیید شود)

---

## 📝 دستورات حذف

```bash
# حذف فایل‌های مطمئن
rm -rf src/data/legacy
rm landingpage.txt
rm "FLEX APP - Shortcut.lnk"

# حذف test-app (بعد از تأیید)
rm -rf test-app
```

---

**آماده برای تأیید و حذف!** 🚀


