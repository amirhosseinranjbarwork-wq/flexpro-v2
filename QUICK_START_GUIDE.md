# 🚀 راهنمای سریع راه‌اندازی FlexPro v2

این راهنما برای کسانی است که می‌خواهند به سرعت پروژه را اجرا کنند.

---

## ⚡ راه‌اندازی در 3 مرحله

### مرحله 1: بررسی پیش‌نیازها ✅

قبل از شروع، مطمئن شوید این برنامه‌ها نصب هستند:

- ✅ **Python 3.8+** → [دانلود](https://www.python.org/downloads/)
- ✅ **Node.js 18+** → [دانلود](https://nodejs.org/)

**بررسی نسخه:**
```bash
python --version    # باید 3.8 یا بالاتر باشد
node --version      # باید 18 یا بالاتر باشد
```

### مرحله 2: اجرای اسکریپت راه‌اندازی 🎯

#### روی Windows:
1. دابل کلیک روی `start-local.bat`
2. صبر کنید تا Backend و Frontend بالا بیایند
3. تمام! 🎉

#### روی Linux/Mac:
```bash
./start-local.sh
```

### مرحله 3: دسترسی به برنامه 🌐

پس از اجرای موفق، این آدرس‌ها در دسترس هستند:

- 🎨 **Frontend (رابط کاربری)**: http://localhost:5173
- 📊 **Backend (API)**: http://localhost:8000
- 📚 **API Docs**: http://localhost:8000/docs
- 💚 **Health Check**: http://localhost:8000/health

---

## 🎯 اولین استفاده

### 1. ورود به سیستم
- آدرس Frontend را در مرورگر باز کنید
- به صورت پیش‌فرض بدون نیاز به لاگین وارد می‌شوید
- داشبورد اصلی نمایش داده می‌شود

### 2. ساخت اولین برنامه تمرینی

**گام 1:** کلیک روی تب "Training Panel"

**گام 2:** انتخاب حرکت
- از فیلترهای سمت چپ استفاده کنید
- گروه عضلانی مورد نظر را انتخاب کنید (مثلاً سینه)
- تجهیزات دلخواه را فیلتر کنید
- حرکت مورد نظر را کلیک کنید

**گام 3:** تنظیم پارامترها
- سیستم تمرینی را انتخاب کنید (Straight Set, Drop Set, ...)
- تعداد ست و تکرار را تعیین کنید
- وزنه را وارد کنید (اختیاری)
- استراحت بین ست‌ها را تنظیم کنید
- RPE یا RIR را مشخص کنید (اختیاری)
- روی "ذخیره حرکت" کلیک کنید

**گام 4:** ساخت برنامه کامل
- چند حرکت دیگر اضافه کنید
- ترتیب حرکات را با Drag & Drop تغییر دهید
- برنامه را ذخیره کنید

---

## 🛠️ راه‌اندازی دستی (اختیاری)

اگر می‌خواهید به صورت دستی Backend و Frontend را اجرا کنید:

### Backend:
```bash
cd flexpro-ai-service
python -m venv venv

# فعال‌سازی:
# Windows: venv\Scripts\activate
# Linux/Mac: source venv/bin/activate

pip install -r requirements.txt
python -m app.main
```

### Frontend:
```bash
# در پوشه اصلی:
npm install
npm run dev
```

---

## 🗄️ دیتابیس

### مکان دیتابیس
دیتابیس SQLite در این مسیر قرار دارد:
```
flexpro-ai-service/flexpro.db
```

### بک‌آپ گرفتن
برای بک‌آپ کافیست فایل `flexpro.db` را کپی کنید:
```bash
cp flexpro-ai-service/flexpro.db backup/flexpro-backup-$(date +%Y%m%d).db
```

### بازیابی
برای بازیابی، فایل بک‌آپ را جایگزین کنید:
```bash
cp backup/flexpro-backup-YYYYMMDD.db flexpro-ai-service/flexpro.db
```

### ریست کردن دیتابیس
اگر می‌خواهید دیتابیس را از اول بسازید:
```bash
# حذف دیتابیس فعلی:
rm flexpro-ai-service/flexpro.db

# راه‌اندازی مجدد Backend (دیتابیس جدید ساخته می‌شود):
cd flexpro-ai-service
python -m app.main
```

---

## ❓ مشکلات رایج

### مشکل 1: Backend اجرا نمی‌شود
**علت:** Python نصب نیست یا نسخه قدیمی است

**راه‌حل:**
```bash
python --version  # بررسی نسخه
# اگر زیر 3.8 است، Python جدید نصب کنید
```

### مشکل 2: Frontend اجرا نمی‌شود
**علت:** Node.js نصب نیست یا npm کار نمی‌کند

**راه‌حل:**
```bash
node --version  # بررسی نسخه
npm --version   # بررسی npm

# اگر مشکل داشت، node_modules را پاک کنید:
rm -rf node_modules
npm install
```

### مشکل 3: Port 8000 یا 5173 در حال استفاده است
**علت:** یک برنامه دیگر از این Port استفاده می‌کند

**راه‌حل:**
```bash
# Windows:
netstat -ano | findstr :8000
taskkill /PID <شماره_پروسه> /F

# Linux/Mac:
lsof -i :8000
kill -9 <شماره_پروسه>
```

### مشکل 4: دیتابیس خطا می‌دهد
**علت:** فایل دیتابیس خراب شده

**راه‌حل:**
```bash
# حذف و ساخت مجدد:
rm flexpro-ai-service/flexpro.db
# سپس Backend را دوباره اجرا کنید
```

### مشکل 5: Backend به Frontend وصل نمی‌شود
**علت:** CORS یا آدرس API اشتباه است

**راه‌حل:**
1. مطمئن شوید Backend در حال اجراست: http://localhost:8000/health
2. فایل `.env` را بررسی کنید:
```bash
VITE_API_URL=http://localhost:8000
```
3. Frontend را دوباره اجرا کنید

---

## 📚 منابع بیشتر

- 📖 **README کامل**: [README.md](README.md)
- 📐 **معماری**: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)
- 🛠️ **راهنمای توسعه**: [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)
- 🎉 **گزارش بازسازی**: [PROJECT_REFACTOR_COMPLETE.md](PROJECT_REFACTOR_COMPLETE.md)

---

## 🆘 نیاز به کمک؟

اگر مشکلی داشتید:

1. ✅ این راهنما را دوباره بخوانید
2. ✅ بخش "مشکلات رایج" را چک کنید
3. ✅ لاگ‌های Backend و Frontend را بررسی کنید
4. ✅ یک Issue در GitHub باز کنید

---

## 🎯 مرحله بعدی

حالا که سیستم اجرا شد، می‌توانید:

1. ✅ برنامه‌های تمرینی ایجاد کنید
2. ✅ رژیم غذایی طراحی کنید
3. ✅ مکمل‌ها را مدیریت کنید
4. ✅ گزارش‌های پیشرفت ببینید
5. ✅ از ویژگی‌های پیشرفته استفاده کنید

---

<div align="center">

**💪 موفق باشید!**

به دنیای FlexPro خوش آمدید 🎉

[⬆ بازگشت به بالا](#-راهنمای-سریع-راه‌اندازی-flexpro-v2)

</div>
